<UserControl x:Class="MyTools.Desktop.Components.NodePluginDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <Grid>
        <Border Background="{DynamicResource SurfaceBrush}"
                CornerRadius="8">
            <wv2:WebView2 x:Name="PluginBrowser" />
        </Border>
        <Border x:Name="HandshakeErrorOverlay"
                Panel.ZIndex="1"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="8"
                Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        MaxWidth="420"
                        Margin="24">
                <TextBlock x:Name="HandshakeErrorTitle"
                           TextAlignment="Center"
                           FontSize="16"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock x:Name="HandshakeErrorDetail"
                           Margin="0,8,0,0"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Opacity="0.7"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
