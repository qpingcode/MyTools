<Window x:Class="MyTools.Desktop.Views.UpdateCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:loc="clr-namespace:MyTools.Desktop.Localization"
        mc:Ignorable="d"
        Title="{loc:Loc CheckForUpdates, DefaultValue=Check for Updates}"
        Height="240"
        Width="440"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}"/>
            <Setter Property="Padding" Value="20,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <!-- Checking state: indeterminate progress + cancel -->
        <Grid x:Name="CheckingState" Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       HorizontalAlignment="Left"
                       Text="{loc:Loc CheckingForUpdates, DefaultValue=Checking for updates...}"
                       FontSize="15"
                       Margin="0,0,0,16"/>

            <ProgressBar Grid.Row="1"
                         IsIndeterminate="True"
                         Height="6"
                         Margin="0,0"/>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{loc:Loc Cancel, DefaultValue=Cancel}"
                        Style="{StaticResource CancelButtonStyle}"
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Result state: message + action buttons -->
        <Grid x:Name="ResultState" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="ResultMessage"
                       Grid.Row="0"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       TextAlignment="Left"
                       TextWrapping="Wrap"
                       FontSize="14"
                       Margin="0,0,0,16"/>

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="DownloadButton"
                        Content="{loc:Loc UpdateDownloadAndRestart, DefaultValue=Download and Restart}"
                        Style="{StaticResource ModernButton}"
                        Margin="0,0,10,0"
                        Visibility="Collapsed"
                        Click="DownloadButton_Click"/>
                <Button x:Name="OkButton"
                        Content="{loc:Loc Confirm, DefaultValue=OK}"
                        Style="{StaticResource ModernButton}"
                        Click="OkButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Downloading state: determinate progress + percent -->
        <Grid x:Name="DownloadingState" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="DownloadingText"
                       Grid.Row="0"
                       HorizontalAlignment="Left"
                       FontSize="15"
                       Margin="0,0,0,16"/>

            <ProgressBar x:Name="DownloadProgress"
                         Grid.Row="1"
                         Minimum="0"
                         Maximum="100"
                         Height="14"
                         Margin="0,0"/>
        </Grid>
    </Grid>
</Window>
