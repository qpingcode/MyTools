{
  "name": "mytools-plugin-browser-search",
  "version": "0.1.0",
  "private": true,
  "description": "Search Chrome, Edge, and Firefox bookmarks and history.",
  "type": "commonjs",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run check && node build-plugin.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/browser-search.test.mjs"
  },
  "dependencies": {
    "@qping/plugin-bus": "0.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.25.8",
    "esbuild-plugin-copy": "^2.1.1",
    "rimraf": "^6.0.1",
    "typescript": "^7.0.2"
  }
}
