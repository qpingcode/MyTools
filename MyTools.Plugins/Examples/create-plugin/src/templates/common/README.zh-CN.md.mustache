# {{name}}

MyTools 插件 `{{pluginId}}`。[English](README.md)

## 功能

- 简要说明这个插件解决的问题。
- 列出主要命令、别名或用户操作流程。

## 使用方法

1. 打开 MyTools。
2. 通过配置的别名、热键或全局搜索结果调用插件。
3. 在此补充插件特有的配置方法和使用示例。

## 开发

```bash
npm install
npm run watch
```

## 发布到 MyTools Hub

提高 `plugin.json` 中的 `version`（必须高于已发布版本），然后运行：

```bash
npm run build
npm run publish:hub
```

发布需要登录 MyTools Hub（`%AppData%/MyTools.Desktop/HubSession.json`），也可以设置 `MYTOOLS_HUB_TOKEN` / `MYTOOLS_HUB_URL`。
